## Coffee bot
*TODO: better documentation...*

`main.py` will start running the Telegram bot and creates a thread for updating the coffee level values. The script could be modified to run the Telegram bot separately and fetching the coffee data from an API. Since this is still in small scale, everything is basically connected together, but better structure to the program should be implemented in the future.
